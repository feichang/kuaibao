div(class="navbar navbar-fixed-top")
	div(class="navbar-inner")
		div(class="container")
			a(class="btn btn-navbar",data-toggle="collapse",data-target=".nav-collapse")
				span(class="icon-bar")
				span(class="icon-bar")
				span(class="icon-bar")
			a(class="brand logo",href="#") 快报平台
			div(class="nav-collapse")
				ul(class="nav")
					li(class="active")
						a(href="/") 今日快报
					li
						a(href="/history") 往期快报
					li
						a(href="/share") 分享快报
					li
						a(href="/feed") 订阅快报
			a(class="powerby",href="http://veryued.org") Power by 飞长
div(class="container")
	div(class="alert alert-info")
		只要订阅快报，快报平台每天晚间12:00左右会将今日的快报整理发送到你的邮箱中，轻松学习；如果你乐于分享，欢迎你将每日发现分享给UED的所有同学们。
			
	div(class="row-fluid")
		div(class="span12")
			table(class="table table-striped")
				thead
					tr
						th #
						th 标题
						th 分享者
						th 备注
						th 标签
				tbody
					- if (result.length)
							- result.forEach(function(item){
								- if (item.title && item.url)
									tr
										td #{item.date.getFullYear()+'-'+item.date.getMonth()+1 + '-' +item.date.getDate()}
										td
											a(href="#{item.url}",target="_blank")
												#{item.title}
										td #{item.author}
										td #{item.note}
										td #{item.tag}
							- })

script(src="../javascripts/jquery.js")
script(src="../bootstrap/js/bootstrap.min.js")
script(src="../bootstrap/js/bootstrap-modal.js")